# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment-specific config
.env

# MCP server configuration — TRACKED: contains only $ENV placeholders, no
# real secrets. Must be in the image for CloudBase git builds.
mcp_servers.json

# MCP server moved to sibling repo (../mcp-server/); ignore remnants
mcp/

# Office temp files
~$*

# IDE / tool caches
.cache/

# Test artifacts
chunks_report.txt

# CloudBase deploy package (generated)
cloudbase-deploy.zip
