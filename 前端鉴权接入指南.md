# AI Note 前端鉴权接入指南（Supabase JWT）

> 适用客户端：Android（BananaNote，supabase-kt + Retrofit）、MiniProgram（uni-app）
> 后端：`backend`（FastAPI），Base URL `/api/v1`

---

## 1. 背景：身份边界从"前端传"改为"token 推导"

改版前，前端把 `user_id` 放在 body / query 里传给后端，后端直接信——**任何人都能伪造 `user_id` 看到别人的会话和任务**。

改版后，后端**不再信任客户端传的 `user_id`**，改为从 Supabase 登录签发的 **JWT access token** 里解析：

```
客户端登录 Supabase → 拿到 access_token (JWT)
   → 每个业务请求带  Authorization: Bearer <access_token>
   → 后端从 Supabase 项目的 JWKS 端点取 JWT Signing Keys（ES256 公钥）验签
   → 从 token 的 sub claim 取出真实 user_id（就是 Supabase 的 user UUID）
```

**`user_id` 是凭证推导的结果，不是客户端声明的属性。** 请求里再传 `user_id` 参数会被后端忽略。

---

## 2. 后端契约（前端必须遵守）

### 2.1 请求头

所有业务接口（chat / chat/stream / chat/resume / tasks/*）都要带：

```
Authorization: Bearer <access_token>
```

`<access_token>` 是 Supabase 登录后拿到的 access token（JWT，用项目 JWT Signing Keys 的 ES256 签名，短效 ~1 小时，可刷新）。

### 2.2 三种情形

| 请求状态 | 后端 user_id 取值 | HTTP 结果 |
|---|---|---|
| 无 `Authorization` 头 | `"default"`（未登录兜底） | 200 |
| 带 `Bearer <有效 token>` | token 的 `sub`（Supabase user UUID） | 200 |
| 带 `Bearer <无效/过期 token>` | — | **401** |
| 后端未配置 `SUPABASE_URL`（开发模式） | 一律 `"default"` | 200 |

### 2.3 需要遵守的规则

1. **`user_id` 参数全部移除**（body、query、form 里的都不要传了，传了也被忽略）
2. **401 = token 无效或过期** → 前端应引导重新登录（或触发 token 刷新后重试一次）
3. **未登录也能用**：不带 header 时后端按 `"default"` 用户处理（匿名兜底，现状保留）

---

## 3. Android（BananaNote）改动

### 3.1 获取 access_token

工程已集成 supabase-kt（`Auth` 已安装），登录流程**不用改**，登录成功后直接取 token：

```kotlin
import io.github.jan.supabase.auth.auth

// 登录成功后 / 每次请求前，从当前会话取 access_token
val accessToken: String? = supabase.auth.currentSessionOrNull()?.accessToken
```

`AuthViewModel` 里已有的 `supabase.auth.currentSessionOrNull()` 就是这个会话——补上 `.accessToken` 即可。

### 3.2 OkHttp 拦截器统一加请求头

**不要**在每个调用点手动加 header——用拦截器一次到位。新建 `AuthInterceptor`：

```kotlin
package com.example.banananote.core.domain.network

import com.example.banananote.common.ui.supabase.SupabaseClientProvider
import io.github.jan.supabase.auth.auth
import okhttp3.Interceptor
import okhttp3.Response

class AuthInterceptor : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        // 每次请求现取 token（supabase-kt 会自动刷新，别缓存过期值）
        val token = SupabaseClientProvider.client.auth.currentSessionOrNull()?.accessToken
        val request = if (token != null) {
            chain.request().newBuilder()
                .header("Authorization", "Bearer $token")
                .build()
        } else {
            chain.request() // 未登录：不带 header → 后端按 default 处理
        }
        return chain.proceed(request)
    }
}
```

把拦截器注册到 Retrofit 用的 `OkHttpClient`（在 `AppModule` / DI 里）：

```kotlin
val client = OkHttpClient.Builder()
    .addInterceptor(AuthInterceptor())
    // ... 原有配置
    .build()
```

### 3.3 移除 `user_id` 参数

**`ChatApiService.kt`** —— 删掉所有 `user_id`：

```kotlin
// 改前
@Multipart @Streaming @POST("api/v1/chat/stream")
suspend fun sendStream(
    @Part("message") message: RequestBody,
    @Part("user_id") user_id: RequestBody,          // ← 删
    @Part("session_id") session_id: RequestBody,
    ...
)

// 改后
@Multipart @Streaming @POST("api/v1/chat/stream")
suspend fun sendStream(
    @Part("message") message: RequestBody,
    @Part("session_id") session_id: RequestBody,
    ...
)
```

同样删掉：
- `listTasks(@Query("user_id") ...)` → 删参数
- `deleteTask(@Query("user_id") ...)` → 删参数（`session_id` 保留）
- `updateTask(@Query("user_id") ...)` → 删参数（`session_id` 保留）

**模型类**（`ChatRequest` / `ResumeRequest`）—— 删除 `user_id` 字段：

```kotlin
// 改前
data class ChatRequest(
    val message: String,
    val user_id: String,      // ← 删
    val language: String? = null,
)

// 改后
data class ChatRequest(
    val message: String,
    val language: String? = null,
)
```

> `session_id` 保留——它仍是前端生成、用来定位会话线程的（`thread_id`），与身份无关。

### 3.4 401 处理

在 Retrofit 的错误处理里判断 401：

```kotlin
if (e is HttpException && e.code() == 401) {
    // token 无效/过期 → 引导重新登录（或刷新 token 后重试一次）
    sessionStore.clearSession()
    // 跳转登录页
}
```

### 3.5 SessionStore 里的 user_id

`AuthViewModel.publishUserId()` 把 Supabase user id 写进 `SessionStore`、调用点又把它塞回请求的写法**已经不需要了**（后端不再读它）。可以删掉 `publishUserId` 及所有 `sessionStore.userId` 的使用；`SessionStore` 本身若只为此保留也可一并清理。

---

## 4. MiniProgram（BananaNote-MiniProgram）改动

工程用统一封装 `src/services/request.js`（`uni.request`），改起来最省事——**只改一处**。

### 4.1 请求头统一注入

MiniProgram 目前是"跳过式登录"，未集成 Supabase。接入真实登录后，把 token 存起来，并在 `request.js` 里统一注入：

```js
// request.js
import { BASE_URL, TIMEOUT } from './config'

let accessToken = ''          // 登录成功后 setAccessToken(token) 写入
export function setAccessToken(t) { accessToken = t }

export function request({ url, method = 'GET', data = null, form = false }) {
  return new Promise((resolve, reject) => {
    const opts = {
      url: url.startsWith('http') ? url : BASE_URL + url,
      method,
      data,
      timeout: TIMEOUT,
      header: {},             // ← 统一 header，取代原来的按 Content-Type 设置
      success: (res) => {
        if (res.statusCode >= 200 && res.statusCode < 300) {
          resolve(res.data)
        } else {
          reject(new Error(`HTTP ${res.statusCode}: ${JSON.stringify(res.data)}`))
        }
      },
      fail: (err) => reject(new Error(err.errMsg || 'network error')),
    }

    // 身份：登录过就带 token，没登录就不带（后端按 default 兜底）
    if (accessToken) {
      opts.header['Authorization'] = `Bearer ${accessToken}`
    }
    // 未登录也允许请求；401 时在 fail 分支提示重新登录

    if (form) {
      opts.header['Content-Type'] = 'application/x-www-form-urlencoded'
    } else if (data) {
      opts.header['Content-Type'] = 'application/json'
    }
    uni.request(opts)
  })
}
```

### 4.2 移除 `user_id`

- `src/services/chat.js`：`sendChat` / `sendAudio` / `resumeChat` 删掉 `user_id`
- `src/services/tasks.js`：`listTasks` / `deleteTask` / `updateTask` 删掉 `user_id`（`session_id` 保留）
- `src/stores/chat.js`、`src/stores/tasks.js`：调用处删掉 `user_id: sessionStore.userId`（`sessionStore` 若无其他用途可清）

---

## 5. 迁移清单

| 项 | Android | MiniProgram |
|---|---|---|
| 登录后取 access_token | `client.auth.currentSessionOrNull()?.accessToken` | 登录响应里存 token |
| 统一注入 `Authorization: Bearer` | OkHttp `AuthInterceptor` | `request.js` header |
| 删 `ChatApiService` 的 `user_id` | ✅ | — |
| 删 `chat.js` / `tasks.js` 的 `user_id` | — | ✅ |
| 删 model 类 / 调用处的 `user_id` | ✅ | ✅ |
| 401 → 引导重新登录 | ✅ | ✅ |
| `session_id` 保留 | ✅ | ✅ |

---

## 6. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 返回 401 | token 无效/过期，或后端 `SUPABASE_URL` 指向的项目与前端登录的项目不一致 | 重新登录；核对两端是同一个 Supabase 项目 |
| 一直按 `default` 用户处理（登录了也没生效） | 请求没带 header，或后端没配 `SUPABASE_URL`（开发模式） | 检查拦截器是否生效；后端 `.env` 配 `SUPABASE_URL` |
| 之前传的 `user_id` 不生效 | 后端已忽略该参数 | 移除所有 `user_id` 传参，改用 header |
| token 还没到 1 小时就 401 | 会话被登出 / refresh token 失效 | 重新登录（supabase-kt 通常会自动刷新） |

---

## 7. 后端验证（联调自测）

在 `.env` 配好 `SUPABASE_URL` 后，可用 curl 自测：

```bash
# 无 token → 200，default 用户
curl -X POST http://localhost:8000/api/v1/chat \
  -F "session_id=test-s1" -F "message=hi"

# 带无效 token → 401
curl -X POST http://localhost:8000/api/v1/chat \
  -H "Authorization: Bearer not-a-jwt" \
  -F "session_id=test-s2" -F "message=hi"

# 带有效 token → 200，sub 为用户 UUID
curl -X POST http://localhost:8000/api/v1/chat \
  -H "Authorization: Bearer <supabase_access_token>" \
  -F "session_id=test-s3" -F "message=hi"
```

（后端 `SUPABASE_URL` 留空时，第三种也会按 default 处理——那是开发模式。）
